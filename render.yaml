# Render Blueprint — one-file IaC for the StudySync backend.
# Deploy: push to GitHub, then Render Dashboard -> New -> Blueprint -> pick this repo.
#
# Free-tier set: API + realtime + managed Postgres + Redis (Key Value).
# The Celery worker has NO free plan on Render (workers are paid-only), so it is
# documented at the bottom — enable it on a paid plan, or run it locally against
# the production DATABASE_URL/REDIS_URL. The worker only powers nightly rollups;
# the live API + realtime demo works fully without it.

databases:
  - name: studysync-db
    plan: free
    databaseName: studysync
    user: studysync

services:
  # --- Redis / Key Value (presence, pub/sub, cache, Celery broker) ---
  - type: keyvalue
    name: studysync-redis
    plan: free
    ipAllowList: []          # internal-only
    maxmemoryPolicy: noeviction

  # --- REST API (FastAPI). Runs migrations on start, then serves. ---
  - type: web
    name: studysync-api
    runtime: docker
    plan: free
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /health
    # Render execs dockerCommand directly (no shell operators), so chaining lives
    # in a script: migrate, then serve.
    dockerCommand: sh start-api.sh
    envVars:
      - key: ENVIRONMENT
        value: prod
      - key: LOG_JSON
        value: "true"
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGINS
        sync: false          # set to the frontend origin, e.g. ["https://studysync.vercel.app"]
      - key: DATABASE_URL
        fromDatabase:
          name: studysync-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: studysync-redis
          property: connectionString

  # --- Realtime presence (python-socketio); scale by raising the instance count ---
  - type: web
    name: studysync-realtime
    runtime: docker
    plan: free
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    dockerCommand: sh start-realtime.sh
    healthCheckPath: /health
    envVars:
      - key: ENVIRONMENT
        value: prod
      - key: LOG_JSON
        value: "true"
      - key: DATABASE_URL
        fromDatabase:
          name: studysync-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: studysync-redis
          property: connectionString
      - key: JWT_SECRET     # must match the API's secret to validate its tokens
        fromService:
          type: web
          name: studysync-api
          envVarKey: JWT_SECRET

# --- Celery worker + beat (paid-only on Render; not in the free set) ----------
# Add this under `services:` on a paid plan to enable nightly aggregation +
# leaderboard rollups. Locally you can instead run it against the prod URLs:
#   DATABASE_URL=... REDIS_URL=... uv run celery -A app.workers.celery_app worker -B -l info
#
#  - type: worker
#    name: studysync-worker
#    runtime: docker
#    plan: starter            # workers have no free plan
#    dockerfilePath: ./backend/Dockerfile
#    dockerContext: ./backend
#    dockerCommand: uv run --no-dev celery -A app.workers.celery_app worker -B -l info
#    envVars:
#      - key: DATABASE_URL
#        fromDatabase: { name: studysync-db, property: connectionString }
#      - key: REDIS_URL
#        fromService: { type: keyvalue, name: studysync-redis, property: connectionString }
